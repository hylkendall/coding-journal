## 2026-07-28(W2D2·周二)
- 今日主题:表单

- 学了什么:
1 form 元素 用于收集用户信息
  action 属性 指定表单提交后发送位置
2 input 空元素 收集
  type 属性 定义输入数据类型
  id 属性 与label中for关联
  placeholder 属性 输入提示
  required 属性 指定表单内容符合type格式的要求
  minlenght maxlength 属性用于设置电子邮件输入的最小和最大字符长度
  focused disabled readonly 表单的三种状态属性
3 label 元素 标签
  for 属性 与input中id关联

4 显式关联(input与label用for与id)
  例：
  <form action="">
    <label for="email"> Email Address: </label>
    <input type="email" id="email" />
  </form>

5 button 元素 按键
  type 有 button submit reset
  value 属性 显示按钮的文本

6 酒店反馈表单实例

- 明天要做:表格

- 今日状态/心情(1-5):5